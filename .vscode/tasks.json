{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "compile-extension",
      "type": "shell",
      "command": "npm run compile",
      "options": {
        "cwd": "${workspaceFolder}/src/extension"
      },
      "problemMatcher": ["$tsc"],
      "group": "build"
    }
  ]
}
